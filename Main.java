public class Main {
    public static void main(String[] args) {
        // Call the showMenu method directly
        MainMenu menu = new MainMenu();
        menu.showMenu();  // Calling the method to show menu
    }
}